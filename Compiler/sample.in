int main()
{
    int id1, i, j=10;
	
	float id2;
	bool _id3[5];
	
	char cnt;
    id2=3.85+9*11;
    id1=50; //123123
	for(i = 0; i <= 10 & j >= 0; ++i)
		j--;
	do{
		i=i+1-2.3*4/10%2;
	}while(false);
    while (i>j || id1 < id2)
    {      
		cnt=cnt+1;
        if (i == j)
			break;
		else if (i | j){ @@
			j=j+1;
			continue;
		}
		else
			j=j-1;
    }
}